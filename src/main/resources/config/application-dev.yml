# ===================================================================
# Spring Boot configuration for the "dev" profile.
#
# This configuration overrides the application.yml file.
#
# More information on profiles: https://www.jhipster.tech/profiles/
# More information on configuration properties: https://www.jhipster.tech/common-application-properties/
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

logging:
  level:
    ROOT: DEBUG
    tech.jhipster: DEBUG
    org.hibernate.SQL: DEBUG
    net.jojoaddison: DEBUG

management:
  zipkin: # Use the "zipkin" Maven profile to have the Spring Cloud Zipkin dependencies
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0 # report 100% of traces

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false # we use Webpack dev server + BrowserSync for livereload
  jackson:
    serialization:
      indent-output: true
  cloud:
    consul:
      config:
        fail-fast: false # if not in "prod" profile, do not force to use Spring Cloud Config
        format: yaml
        profile-separator: '-'
      discovery:
        prefer-ip-address: true
        tags:
          - profile=${spring.profiles.active}
          - version=#project.version#
          - git-version=${git.commit.id.describe:}
          - git-commit=${git.commit.id.abbrev:}
          - git-branch=${git.branch:}
      host: localhost
      port: 8500
  # Spring Boot 4 moved the Mongo properties from spring.data.mongodb.* to spring.mongodb.*;
  # the old prefix is deprecated at error level and is no longer bound.
  mongodb:
    uri: mongodb://localhost:27017/patientGateway?waitQueueMultiple=1000
  mail:
    host: localhost
    port: 25
    username:
    password:
  messages:
    cache-duration: PT1S # 1 second, see the ISO 8601 standard
  thymeleaf:
    cache: false

server:
  port: 5505

# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  gateway:
    rate-limiting:
      enabled: false
      limit: 100000
      duration-in-seconds: 3600
  # CORS is only enabled by default with the "dev" profile
  cors:
    # Allow Ionic for JHipster by default (* no longer allowed in Spring Boot 2.4+)
    #
    # The last entry is PORTLESS AND DELIBERATE. `https://localhost` is the Capacitor webview's own
    # origin on a handset, and that is read off what ships rather than off the file that authors it:
    # the release build's merged config (android/app/build/intermediates/assets/release/
    # mergeReleaseAssets/capacitor.config.json) carries `androidScheme: https` with NO hostname and NO
    # server.url, @capacitor/android 8.5.0 defaults the hostname to `localhost` (CapConfig.java:38),
    # and Bridge.java:625 composes `scheme + "://" + authority` with no port. `https://localhost:8100`
    # beside it is only ever `ionic serve` on a workstation. This entry reads like a truncated copy of
    # that one and is not one.
    #
    # The app reaches every other endpoint through CapacitorHttp, which is native and never
    # preflights, so CORS applies to exactly one request: the membership stream, which must be read
    # through `window.CapacitorWebFetch` because the patched fetch cannot stream a response body and
    # hangs for ever on SSE. Its `Authorization` header forces a preflight, and without this origin
    # the stream is refused 403 before a frame is sent — measured against the quality gateway, where
    # `https://localhost:8100` got a 200 from the same endpoint in the same run.
    #
    # THE ORIGIN IS NECESSARY, NOT SUFFICIENT: that same preflight carries
    # `Access-Control-Request-Headers: authorization`, so `allowed-headers` below must keep admitting
    # it — narrowing that line refuses the stream with this origin still listed. Spring gives
    # `allowed-methods` a [GET, HEAD] default when it is absent and gives `allowed-headers` no default
    # at all, which is why only the headers line is guarded by a test. That asymmetry holds ONLY
    # because nothing calls `CorsConfiguration.applyPermitDefaultValues()` — WebConfigurer:39 passes
    # these properties through untouched, and that method would default absent headers to permit-all.
    #
    # Auth here is header-borne, not cookie-borne, so the allowance creates no ambient-credential
    # CSRF surface — `allow-credentials: true` below is pre-existing and attaches nothing that
    # authenticates: the only ResponseCookie in this service is LocaleConfiguration's locale cookie,
    # and no CSRF token cookie is issued either, since CSRF is disabled (SecurityConfiguration:56).
    # That enumeration is the argument; CSRF being off is not evidence of safety, it is what would
    # make an ambient credential exploitable IF one existed. docs/backlog.md item 67 carries the
    # argument and the architect's decision.
    #
    # THIS FILE IS QUALITY'S CORS POLICY, not just a developer's: the quality stack runs
    # SPRING_PROFILES_ACTIVE=dev,test and overrides no JHIPSTER_CORS_* variable. PRODUCTION IS NOT
    # COVERED — it has no cors block at all, so the shipped app against patient.abofonsa.com is still
    # refused. That is deferred deliberately and tracked as item 77, not an omission to tidy up here.
    allowed-origins: 'http://localhost:8100,https://localhost:8100,http://localhost:9000,https://localhost:9000,https://localhost'
    # Enable CORS when running in GitHub Codespaces
    allowed-origin-patterns: 'https://*.githubpreview.dev'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count,X-${jhipster.clientApp.name}-alert,X-${jhipster.clientApp.name}-error,X-${jhipster.clientApp.name}-params'
    allow-credentials: true
    max-age: 1800
  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        # DEVELOPMENT AND TEST ONLY. This value is committed, therefore public, therefore not a secret — it is
        # here so `./mvnw` works with no setup. It is deliberately the SAME string in hc-patient-service and
        # hc-patient-gateway: the gateway mints tokens and the service validates them, and until 2026-08-05 the two
        # repos shipped DIFFERENT committed keys, so a relayed token failed validation downstream in every
        # environment that did not override both.
        #
        # Production reads JWT_BASE64_SECRET from the environment (see application-prod.yml) and the application
        # refuses to start without it. Never copy this value anywhere that matters.
        base64-secret: G5oVDNDxRIh5PtBW0J+79wSUU4KuLJsDcXyo36DsTGIjSwVWjQBAXOXrCPDjf8RKqfghIRoBi2/H1IbqyAfasg==
        # Shortened 2026-08-05. Was 24 hours / 30 days.
        #
        # Authentication here is stateless: no jti, no blocklist, no server-side session, and logout is a client-side
        # localStorage.removeItem. Nothing can revoke a token before it expires, so its lifetime IS the exposure
        # window for a stolen one — and the same signing key is shared with hc-admin and hc-professional, so that
        # window spans all three products. A month of it for health records was not a defensible number.
        #
        # 1 hour, and 7 days for "remember me". Real revocation (a jti claim plus a small blocklist collection both
        # decoders consult, or refresh tokens) is the actual fix and is tracked separately; this is the part that
        # costs nothing and shrinks the window by 97%.
        token-validity-in-seconds: 3600
        token-validity-in-seconds-for-remember-me: 604800
  mail: # specific JHipster mail property, for standard properties see MailProperties
    base-url: http://127.0.0.1:5505
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      ring-buffer-size: 512
# ===================================================================
# Application specific properties
# Add your own application properties here, see the ApplicationProperties class
# to have type-safe configuration, like in the JHipsterProperties above
#
# More documentation is available at:
# https://www.jhipster.tech/common-application-properties/
# ===================================================================

# application:
